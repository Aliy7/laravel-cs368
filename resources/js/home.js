// Homepage.js

// Add custom styles to the black section
const blackHeader = document.querySelector('.black-header-back');
if (blackHeader) {
    blackHeader.style.backgroundColor = 'black';
    blackHeader.style.color = 'white';
    blackHeader.style.padding = '40px';
}
